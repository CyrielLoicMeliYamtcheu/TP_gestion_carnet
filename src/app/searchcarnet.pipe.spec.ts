import { SearchcarnetPipe } from './searchcarnet.pipe';

describe('SearchcarnetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcarnetPipe();
    expect(pipe).toBeTruthy();
  });
});
